package com.notifyah.user.entity;

/**
 * User account status enumeration.
 */
public enum UserStatus {
    ACTIVE,      // 활성 계정
    INACTIVE,    // 비활성 계정
    LOCKED,      // 잠긴 계정
    SUSPENDED,   // 정지된 계정
    EXPIRED      // 만료된 계정
} 